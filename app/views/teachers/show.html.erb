<div class="container">

  <p>
    <%= link_to 'Voltar', teachers_path %>
  </p>

  <div class="row">

    <div class="col s4">
      <img class="materialboxed" width="250" src="https://robohash.org/<%= @teacher.name.parameterize %>?bgset=any" data-caption="<%= @teacher.name %>">
    </div>

    <div class="col s8">
      <h3><%= @teacher.name %></h3>
      <h4>Título: <%= @teacher.title %></h4>

      <h5>Nota: <%= @teacher.grade %></h5>
      <h5>Dificuldade: <%= @teacher.difficulty %></h5>
    </div>

  </div>

  <% if @teacher.ratings.any? %>

    <div class="row">
      <div class="col s12">
        <%= link_to 'Avaliar', new_teacher_rating_path(@teacher), class: "btn right" %>
        <h4>Avaliações</h4>
        <p>Total: <%= @teacher.ratings.length %></p>
      </div>

      <div class="row">

        <% @teacher.ratings.each do |rating| %>
          <div class="col s4">

            <%= link_to teacher_rating_path(@teacher, rating) do %>
              <div class="card-panel teal">
                <span class="white-text right"><%= format_date(rating.created_at) %></span>
                <p class="white-text">Teria aula novamente: <%= (rating.take_again ? "Sim" : "Não") %></p>
                <p class="white-text">Dificuldade: <%= rating.difficulty %></p>
                <p class="white-text truncate">Texto: <%= rating.text %></p>
              </div>
            <% end %>
          </div>
        <% end %>

      </div>

    <% else %>

      <div class="row">
        <div class="center-align col s12">
          <h4>Não foi encontrado avaliações :(</h4>
          <i class="material-icons">list</i>
        </div>
      </div>

    <% end %>

  </div>
</div>